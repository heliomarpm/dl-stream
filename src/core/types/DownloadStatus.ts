enum DownloadStatus {
    QUEUED = "queued",
    DOWNLOADING = "downloading",
    PAUSED = "paused",
    COMPLETED = "completed",
    FAILED = "failed",
    CANCELED = "canceled"
}

export default DownloadStatus;
